<header class="header">
  <nav class="nav">
    <a href="/" class="logo">
      Silfrido Benítez
    </a>
    <button class="btn-menu">Menú</button>
    <ul class="nav-list">
      <li class="nav-item">
      </li>
      <li class="nav-item">
        <a href="/#experiencia" class="nav-link">Experencia</a>
      </li>
      <li class="nav-item">
        <a href="/#servicios" class="nav-link">Proyectos</a>
      </li>
      <li class="nav-item">
        <a href="/#sobre-mi" class="nav-link">Sobre mí</a>
      </li>
      <li class="nav-item">
        <a href="#" class="nav-link">Contacto</a>
      </li>
    </ul>
  </nav>
</header>
<style>
  .header{
    max-width: 90%;
		width: 100%;
		margin: 0 auto;
    padding: 1.5rem;
    display: flex;
    align-items: center;
    justify-content: center;
    border-top: 1px solid var(--secundario);
    border-left: 1px solid var(--secundario);
    border-bottom: 1px solid var(--secundario);
    border-right: 1px solid var(--secundario);
  }
  .nav{
    width: 100%;
    display: flex;
    align-items: center;
    justify-content: space-between;
  }
  .logo{
    font-size: 1.5rem;
    color: var(--principal);
    text-decoration: none;
  }
  .nav-list{
    display: flex;
    align-items: center;
    gap: 1.5rem;
    margin-bottom: 0;
    padding-left: 0;
  }
  .nav-link{
    font-size: 1rem;
    color: var(--principal);
    text-decoration: none;
  }
  .btn-menu{
    display: none;
    background: transparent;
    border: none;
    font-size: 1rem;
    color: var(--principal);
  }
  @media screen and (max-width:1100px) {
    .header{
      max-width: calc(100% - 30px);
    }
    .btn-menu{
      margin-left: auto;
      display: block;
    }
    .nav-list{
      display: none;
    }
  }
  @media screen and (max-width:768px) {
    .header{
      max-width: calc(100% - 2px);
    }
    .logo{
      font-size: 1.2rem;
    }
  }
</style>